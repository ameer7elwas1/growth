# إعداد معرف المشروع (Project ID)

## نظرة عامة

تم إضافة نظام لتمييز المستخدمين حسب المشروع. هذا يسمح لعدة مشاريع باستخدام نفس قاعدة البيانات Supabase دون أن تتداخل بيانات المستخدمين.

## الخطوات المطلوبة

### 1. تحديث قاعدة البيانات

قم بتشغيل ملف SQL التالي في Supabase SQL Editor:

```sql
-- الملف: sql/add_project_id_to_users.sql
```

هذا سيضيف عمود `project_id` إلى جدول `users`.

### 2. تحديث ملف الإعدادات

تم تحديث ملف `config.js` لإضافة معرف المشروع. القيمة الافتراضية هي `'growth_iraqcell'`.

يمكنك تغييرها في `config.js`:

```javascript
const SUPABASE_CONFIG = {
  URL: '...',
  ANON_KEY: '...',
  PROJECT_ID: 'your_project_id_here' // غيّر هذا حسب مشروعك
};
```

أو يمكنك تعيينه عبر window variable قبل تحميل الصفحة:

```html
<script>
  window.PROJECT_ID = 'your_project_id_here';
</script>
```

### 3. تحديث المستخدمين الموجودين

إذا كان لديك مستخدمين موجودين في قاعدة البيانات، يجب تحديثهم لربطهم بمشروع معين:

```sql
UPDATE public.users 
SET project_id = 'growth_iraqcell' 
WHERE project_id IS NULL OR project_id = '';
```

## التغييرات التي تمت

### الملفات المعدلة:

1. **config.js** - إضافة `PROJECT_ID`
2. **config/config.example.js** - إضافة `PROJECT_ID` في القالب
3. **index.html** - تصفية المستخدمين حسب `project_id` في تسجيل الدخول
4. **js/app.js** - تصفية المستخدمين حسب `project_id` في تسجيل الدخول
5. **user_management_database.html** - تصفية وعرض وإضافة المستخدمين حسب `project_id`

### الملفات الجديدة:

1. **sql/add_project_id_to_users.sql** - ملف SQL لإضافة عمود `project_id`
2. **PROJECT_ID_SETUP.md** - هذا الملف

## كيفية العمل

- عند تسجيل الدخول: يتم البحث عن المستخدمين الذين يطابقون `username` و `password` و `project_id`
- عند عرض المستخدمين: يتم عرض فقط المستخدمين الذين يطابقون `project_id` الحالي
- عند إضافة مستخدم جديد: يتم ربطه تلقائياً بـ `project_id` الحالي
- عند تحديث/حذف مستخدم: يتم التأكد من أن المستخدم ينتمي إلى `project_id` الحالي

## ملاحظات مهمة

1. **القيمة الافتراضية**: إذا لم يتم تعيين `PROJECT_ID`، سيتم استخدام `'growth_iraqcell'` كقيمة افتراضية.

2. **المستخدمين القديمين**: الكود يدعم المستخدمين القديمين الذين لا يملكون `project_id` في التخزين المحلي (localStorage) للتوافق مع الإصدارات السابقة.

3. **الأمان**: تأكد من أن كل مشروع يستخدم `PROJECT_ID` فريداً لتجنب التداخل.

4. **التخزين المحلي**: المستخدمين في localStorage يتم تصفيتهم أيضاً حسب `project_id`.

## الاختبار

بعد تطبيق التغييرات:

1. تأكد من تشغيل ملف SQL في Supabase
2. تحقق من أن المستخدمين الموجودين لديهم `project_id` صحيح
3. جرب تسجيل الدخول - يجب أن يعمل فقط للمستخدمين من مشروعك
4. جرب إضافة مستخدم جديد - يجب أن يتم ربطه تلقائياً بمشروعك
5. تأكد من أن المستخدمين من مشاريع أخرى لا يظهرون في قائمة المستخدمين

