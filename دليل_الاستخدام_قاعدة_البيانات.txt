# نظام النسخ الاحتياطي لقاعدة البيانات - دليل الاستخدام السريع

## 🎯 الملفات الرئيسية:

### 1. أدوات النسخ الاحتياطي لقاعدة البيانات
- **`database_backup_tool.html`** - واجهة ويب تفاعلية (انقر نقرة مزدوجة)
- **`database_backup_manager.bat`** - قائمة إدارة شاملة
- **`database_backup_simple.ps1`** - سكريبت PowerShell مبسط
- **`database_import.ps1`** - سكريبت استيراد البيانات

### 2. أدوات النسخ الاحتياطي للملفات
- **`easy_backup.bat`** - نسخ احتياطي سريع للملفات
- **`auto_backup_simple.ps1`** - سكريبت PowerShell للملفات

## 🚀 كيفية الاستخدام:

### الطريقة السريعة (واجهة ويب):
1. انقر نقرة مزدوجة على `database_backup_tool.html`
2. اختر العملية:
   - **📤 تصدير البيانات**: تصدير localStorage إلى JSON
   - **📥 استيراد البيانات**: إضافة بيانات من ملف
   - **🔄 استعادة البيانات**: مسح واستبدال البيانات

### الطريقة المتقدمة (قائمة الإدارة):
1. انقر نقرة مزدوجة على `database_backup_manager.bat`
2. اختر من القائمة:
   - فتح أداة النسخ الاحتياطي
   - نسخ احتياطي سريع للملفات
   - نسخ احتياطي لقاعدة البيانات
   - استيراد البيانات من ملف

### النسخ الاحتياطي السريع للملفات:
- انقر نقرة مزدوجة على `easy_backup.bat`

## 📁 الملفات المنشأة:

### نسخ قاعدة البيانات:
- `database_backups\export_localStorage_*.html` - سكريبتات التصدير
- `database_backups\import_data_*.html` - سكريبتات الاستيراد
- `database_backups\backup_log.txt` - سجل العمليات

### نسخ الملفات:
- `backups\agents_growth_iraqcell_backup_*.html` - نسخ الملفات
- `backups\backup_log.txt` - سجل نسخ الملفات

## ⚙️ إعداد Supabase (اختياري):

1. افتح `database_backup.ps1`
2. أضف معلومات Supabase:
   ```powershell
   $SupabaseUrl = "https://your-project.supabase.co"
   $SupabaseKey = "your-anon-key"
   ```
3. شغل السكريبت مع المعاملات:
   ```powershell
   .\database_backup.ps1 -SupabaseUrl "your-url" -SupabaseKey "your-key"
   ```

## 🔧 استيراد البيانات:

### من سطر الأوامر:
```powershell
# استيراد عادي
.\database_import.ps1 -ImportFile "path\to\backup.json"

# استعادة كاملة
.\database_import.ps1 -ImportFile "path\to\backup.json" -RestoreMode
```

### من الواجهة:
1. افتح `database_backup_tool.html`
2. اختر "استيراد البيانات" أو "استعادة البيانات"
3. اختر ملف JSON
4. اتبع التعليمات

## 📊 إحصائيات النظام:

- **عدد النسخ الاحتياطية**: يحتفظ بـ 10 نسخ تلقائياً
- **التنظيف التلقائي**: حذف النسخ القديمة عند تجاوز الحد
- **السجلات**: تتبع جميع العمليات
- **التوافق**: يعمل مع جميع المتصفحات الحديثة

## 🆘 استكشاف الأخطاء:

### مشاكل شائعة:
1. **لا يعمل التصدير**: تأكد من تشغيل السكريبت في المتصفح
2. **خطأ في الاستيراد**: تحقق من تنسيق ملف JSON
3. **فشل Supabase**: تحقق من URL و API Key

### ملفات السجل:
- `database_backups\backup_log.txt`
- `database_backups\import_log.txt`
- `backups\backup_log.txt`

## 🎉 تم إنشاء النظام بنجاح!

النظام جاهز للاستخدام ويوفر:
✅ نسخ احتياطية تلقائية للملفات  
✅ تصدير واستيراد بيانات localStorage  
✅ دعم قاعدة البيانات السحابية (Supabase)  
✅ واجهات سهلة الاستخدام  
✅ إدارة النسخ القديمة  
✅ سجلات مفصلة للعمليات  

---
**نصيحة:** ابدأ بتجربة الواجهة الويب (`database_backup_tool.html`) فهي الأسهل للاستخدام!
